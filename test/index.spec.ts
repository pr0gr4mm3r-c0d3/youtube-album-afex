import { describe, it } from 'vitest';
// The two tests marked with concurrent will be run in parallel
describe('Vitest Library', () => {
	it('Check Vitest Library running', () => {
		console.log('Vitest Test Library running');
	});
});
